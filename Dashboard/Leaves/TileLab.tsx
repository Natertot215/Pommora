// The tile grid and its layout engine are Core's; the showcase reaches only UIX, so the stage
// stands empty until a tile primitive lives in the kit.
export function TileLab(): React.JSX.Element {
  return <div className="tile-lab" />
}
