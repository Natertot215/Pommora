## Embedded Pages — Execution Directive

Nathan's verbatim instruction (2026-08-03), preserved against context compaction — copy-paste-safe:

> Once verification rounds confirm the plan is able to be exectuted, with no post-plan erros at-risk, all user-actions considered so that Nathan doesn't interact with the feature and find discrepencies, UIX frustrations, or behavior that shouldn't happen; you're going to provide an in-chat summary of the findings these rounds produced and how they were fixed in the plan. Include these breifly and summary-level so nathan cna understand.
>
> Then, in-chat, give a full explanation of the Feature as-shipped, as if you'd be writing a small product documentation about this feature. This feature will become II. Embeddings in MarkdownPM.md, with mentions of the cross-functional behavior and code intertwined NATURALLY within SurfacePM + Other documents that directly talk about the same things we do here; allign it, don't emphasize.
>
> Once that's done, Give me an in-chat prompt to give you that tells you to execute the plan.
>
> MANDATRORY STEPS include post-phase simplification + post-plan simplification THEN build-breaking. What the simplifiers flag should be noted and reasoned so you can give the build-brakers correct framing rather than them potentially mis-interpreting false positives if your individual reasoning proves valueable.
>
> Once the plan is fully reviewed through code and simplification, you should yourself as the main agent MANUALLY walk through the entire interaction pattern. Create a list of interaction-flow across all sources (SurfacePM embeddings, embeddings when in a page-preview; creating, swapping, deleting embeds ect....) and create a checklist of all of the interaction points you can think of that could be done. Then send an agent to test these, if anything breaks or proves "finnicky" consolidate the most minimal fix.
>
> After that, comes the documentation that must re-visit my ClaudeOS + StudioMD documentation and formatting instructions.
>
> The final gate is after all of this, you give an in-chat summary of everything done to Nathan, the Context.MD is updated accordingly, and the Handoff aswell -- also biasing the same instructions about documentation and framing Ive consistently been reminding you about.
>
> ---
>
> Commit to origin this current branch right before executing the plan in-line, it's been long enough.
> PING nathan's phone after each milestone, any calls you need to make on-the-fly should be done on your best judgement, but CLEARLY flagged in the final report if applicable.
>
> DONT use the playwright tool; drive a live instance test of the test Nexus for all testing.
>
> Save this instruction after confirming it so that incase its lost in an auto-compaction; this request remains in history for me to copy-paste back.
