.DS_Store
*.log
.env
.env.*
*.local

# Editors
.vscode/
.idea/

# React / Node
node_modules/
dist/
out/
release/
coverage/
.vite/
*.tsbuildinfo
*.env
**/build/_dots.png

# Pommora — per-nexus app-internal config
# Skipped from any git-tracked folder used as a dev test nexus
# (e.g. when pointing Pommora at the Studio or a project subfolder)
.nexus/

# Graphify — regeneratable knowledge-graph output (55MB+, rebuilt via `graphify update .`)
graphify-out/

# Playwright MCP — tool console logs / page traces (regeneratable)
.playwright-mcp/

# Claude Code — the project's agentic documentation and tooling. It is its own
# repository inside this one: versioned on this machine, never on the remote.
.claude/